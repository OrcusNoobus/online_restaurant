/**
 * Orders day view — placeholder until T12 builds the polling list + detail
 * panel (003 07-tasks). The shell (auth, nav, logout) is live from T03.
 */
export default function AdminOrdersPage() {
  return (
    <div>
      <h1 className="text-2xl font-bold tracking-tight">Comenzi</h1>
      <p className="mt-4 rounded-2xl border border-dashed border-zinc-300 bg-white p-6 text-sm text-zinc-500 dark:border-zinc-700 dark:bg-zinc-900 dark:text-zinc-400">
        Vizualizarea comenzilor pe zi se construiește în pasul următor (T12).
      </p>
    </div>
  );
}
